FROM node:20-alpine

WORKDIR /app

# 先复制package文件
COPY frontend/package*.json ./

# 安装依赖
RUN npm install

# 复制前端代码
COPY frontend/ .

# 构建生产版本
RUN npm run build

# 暴露端口
EXPOSE 3000

# 使用 vite preview 启动（生产构建）
CMD ["npx", "vite", "preview", "--host", "0.0.0.0", "--port", "3000"]
